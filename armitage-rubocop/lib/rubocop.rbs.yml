plugins:
  - rubocop-on-rbs

inherit_from:
  - ./rbs/style.yml
  - ./rbs/lint.yml
  - ./rbs/layout.yml

RBS:
  Enabled: true
  # NOTE: include option does not work at the moment but we have prepared in advance;
  Include:
    - "**/*.rbs"
